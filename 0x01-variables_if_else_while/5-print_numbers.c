#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print zero to ten
 *
 * Return: zero
 */

int main(void)
{
	/**
	 * n: is number
	 */
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);

	printf("\n");

	return (0);
}
