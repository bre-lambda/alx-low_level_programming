#include "main.h"
/**
 * _abs -Entery point
 * Description-prints the absolute value of int
 * @n: the value recived
 * Return: value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (0 - n);
}
